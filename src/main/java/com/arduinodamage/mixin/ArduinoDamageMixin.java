package com.arduinodamage.mixin;

import org.spongepowered.asm.mixin.Mixin;
import net.minecraft.server.MinecraftServer;

// Placeholder mixin — no behaviour needed; damage detection is done via tick events.
@Mixin(MinecraftServer.class)
public class ArduinoDamageMixin {
}
